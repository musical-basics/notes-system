// Copy this file to `config.local.js` (gitignored) and fill in your Supabase creds.
// The dashboard auto-connects on load so you don't re-paste keys in the modal.
// The key stays on your machine — config.local.js is gitignored and never committed.
window.NOTES_CONFIG = {
  url: "https://YOUR_PROJECT.supabase.co",
  key: "sb_secret_…",   // or a publishable key if you set up RLS
};
